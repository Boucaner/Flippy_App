{
  "name": "flippy",
  "version": "1.0.0",
  "description": "Flippy - Memory Matching Tile Game",
  "scripts": {
    "build": "node scripts/copy.js",
    "cap:sync": "npm run build && npx cap copy android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.2.0"
  }
}
